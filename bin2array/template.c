#include <stdint.h>
#include <pico/platform.h>

uint8_t __in_flash() ESP_FIRMWARE[] = {
    //firmware
};

uint8_t __in_flash() ESP_BOOTLOADER[] = {
    //bootloader
};

uint8_t __in_flash() ESP_PARTITION[] = {
    //partition
};
